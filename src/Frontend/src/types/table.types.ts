export interface DataType {
  key: string | number;
  title: string;
  authors: string;
  abstract: string;
  year: number | string;
  source: string;
  keywords: string;
  doi: string;
  isbn: string;
  issn: string;
  pages: string;
}